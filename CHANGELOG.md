# Changelog

## 2026-09-26（二）

### Added

- **餐厅账户**：第一次打开管理台注册（账户名 + 密码），之后用账户名和密码登录管理台、配对 POS；
  账户下管理多个跑堂。设置里可以改账户名、显示名称和密码；忘了密码用 `ADMIN_TOKEN` 重设。
  以前的管理密码自动变成账户 `admin`（迁移 0055）。
- **实时同步**（`/ws`）：每次写入都推送「哪桌有变化」，管理台看板、桌位和 POS 桌台、点单、结账页立即刷新。
  Cloudflare 上用 Durable Object，Node 上用进程内连接；断线自动重连，期间定时刷新兜底。
- **管理台跑堂实时视图**：每个跑堂在哪台设备、开着哪些桌、本班小票数、营业额、手上现金；
  桌位显示谁正在操作，订单显示哪个跑堂点的、外带取餐号。
- **退菜**：已送厨的菜按份数退掉，必须写原因，后厨打退菜单，进日志，计入跑堂结算（迁移 0056）。
- **沽清**：POS 上一键下架 / 恢复菜品，顾客菜单立即同步。
- **补打小票**：小票副本，印「BELEGKOPIE / 小票副本」。

### Fixed

- Worker 的订单从来没有 `billedAt`，看板上「已结账」标记在线上从不显示；两个后端的订单视图现在是同一份代码。

## 2026-09-26

### Added

- **POS 点餐收银**（`pos.html`，`apps/pos-web`），参照奥地利中餐馆常用的 Gastro-Kassa：
  设备用经理密码配对，跑堂各自 PIN 登录；菜号快速点单、送厨（后厨单中文、无价格）；
  桌号锁（一桌一台设备，90 秒无心跳释放，经理可接管）；转桌；外带取餐号和外带折扣；
  一起结 / 按菜分开结，现金找零、银行卡、代金券混合支付；不可修改的小票和冲销；
  跑堂结算（Kellnerabrechnung）；日结 Z 报表；哈希链交易日志（DEP）按日期导出 JSON / CSV 并校验。
- 管理台设置「跑堂与 POS 设备」：添加跑堂、改 PIN、停用，取消设备配对；公司资料和外带折扣。

### Changed

- 管理台去掉「收银」标签：收银统一在 POS 里做，桌位的账单只打印并跳转到 POS。

### Fixed

- Node 服务器跨域只放行 GET / HEAD / POST（`@fastify/cors` 的默认值），前端和 API 不同源时
  PUT / PATCH / DELETE 全被浏览器拦下。现在与 Worker 一致。

## 2026-09-18

### Fixed

- **三语只做了一半**。这是一家奥地利餐厅，主力客群说德语，但选了 Deutsch 之后只有菜单是德语：
  订单状态显示 `新订单` / `制作中`，下单确认弹 `订单已提交`，服务呼叫写着 `加水`。英文更糟——
  `services` 是 `[id, 中文, 德文]` 三元组，英文用户看到的主标题是中文、副标题是德文。
  现在 7 个订单状态、6 个服务项、5 条操作反馈、员工看板全部走词典；字典从 30 条扩到 55 条。
- 详情卡的三个 `aria-label`（关闭详情 / 菜品详细信息 / 返回）此前恒为中文，德语和英语用户的
  读屏软件会念中文。现在跟随界面语言。

### Changed

- `services` 从 `[id, zh, de]` 元组改为 `{ id, names: { zh, de, en } }`，与菜品和选项的
  三语命名同构；管理台的服务名改为从同一份数据取中文，两处不会再各自漂移。
- `ServiceRequest` 不再持久化 `label`。此前呼叫服务时把当前语言的标签写进本地状态，
  之后切换语言，历史记录仍停留在旧语言。现在只存 `serviceType`，渲染时解析。
- 顾客端状态里的 `serviceMessage`（一条中文字符串）换成 `lastServiceType`，同理。
- 带插值的文案改用 `{name}` 占位模板——德语英语需要分隔符，中文不需要，直接拼接会得到
  `WasserAnfrage gesendet`。

## 2026-09-17

### Changed

- 统一动效节奏：`--ease-out` 和 `--dur-*` 令牌集中在 `:root`，顾客端详情卡的 JS 动画沿用同一组
  数值，遮罩、卡片入场和 3D 翻转现在是一套动作而不是三套。翻转从 0.58s 收到 0.42s。
- 详情卡增加退场动画（`AnimatePresence`）。此前关闭是直接卸载，开有动画、关没有。

### Fixed

- **`prefers-reduced-motion` 对 JS 动画无效**。CSS 里的 reduced-motion 块只能压住 CSS 过渡，
  管不到 motion/react 驱动的详情卡入场和翻转——开了"减少动画"的设备照样看到 0.58s 的翻转。
  改用 `useReducedMotion()` 把这些时长归零，并加了一对 Playwright 用例钉住：开启时翻转在
  250ms 内落位，未开启时 120ms 仍在运动中。
- 所有可点元素补上按下反馈。全局 `-webkit-tap-highlight-color: transparent` 清掉了系统的触摸
  反馈却没有替代，平板上每个按钮按下去都没反应。现在按下有 scale 回弹，且在 reduced-motion
  下关闭。

### Performance

- 移除菜单列表上无用的 3D 上下文：`.stack` 的 `perspective` 和 `.dish-card` 的
  `transform-style: preserve-3d`。详情卡是 `.stack` 的兄弟节点而非后代，这个 3D 上下文没有
  任何作用对象，却让最常滚动的列表为每张卡多一层合成。`.dish-card` 上那条 height/margin
  过渡也一并删除——它服务的是早已被覆盖式详情卡取代的内联展开，且已被后面的同名声明覆盖。
- 背景列表虚化不再做过渡动画。逐帧重算整列的模糊半径是这里最贵的一步，改为模糊直接落位、
  只过渡暗化。

## 2026-09-02

### Fixed

- 管理接口的分类校验不再拒绝含空格的分类；此前 `HOT POT`、`MAIN DISHES`、`WINE APERITIF` 等
  7 个分类下的 45 个 seed 商品在管理台保存时会返回 400，完全改不了。
- 桌号不再硬编码为 `08`：每台设备用 `?table=` 或管理台“本机桌号”设置一次并持久化，下单和
  服务呼叫都使用它；服务端也不再为缺失桌号填默认值，而是直接拒绝。
- 顾客端离线且没有目录缓存时不再回退到 15 条 demo 菜单。那些菜品在真实目录里不存在，据此
  下的单会被服务端永久拒绝；现在显示“菜单暂时不可用”。
- 反向代理部署下的认证限流误伤：新增 `TRUST_PROXY` 配置，未设置时所有请求都算同一来源，
  任何人连错 5 次就会锁住全部管理员。失败计数表也增加了过期清理和条目上限。
- 详情页加入购物车改为累加同一条购物车行（相同菜品 + 相同选项），并且每次打开详情都从
  数量 1、未选选项开始，不再从无关的购物车行预填数量。
- 本机订单历史上限 50 条，不再无限增长后整份写入 localStorage。

### Added

- 管理台新增“订单看板”：消费后端 `/api/orders` 与 `/api/service-requests`，按后端状态机推进
  订单与服务呼叫，WebSocket 实时刷新。此前这些接口在成品 UI 里没有任何调用方，厨房用不了；
  顾客端的“员工看板”只显示本机记录，已在页面上标注。

### Security

- Android 管理 PIN 改用随机盐 + PBKDF2（SHA-256，20 万次迭代；API 26 以下回退 PBKDF2-SHA1）
  存储，替换原先的单轮 SHA-256；旧记录在下次成功解锁时自动升级。
- 升级 fast-uri 修复 GHSA-7p8r-x3mc-p8w7（high）。
- 升级 fastify 到 5.12.1 并把依赖下限提到该版本，修复 GHSA-w2qp-rph6-63g4（schema 校验绕过）
  与 GHSA-3m5p-2c4r-xxw2（trustProxy 跳数下的 `X-Forwarded-*` 伪造）。
- `TRUST_PROXY` 只接受可信代理地址/网段。跳数写法在 fastify 5.12 之后不再解析转发地址——配了
  等于没配，限流仍会锁住所有管理员——`true` 则采信客户端可伪造的最左侧地址；两者现在都在启动
  时报错退出，并有集成测试固定该行为。

### Changed

- 服务请求与打印机接口返回契约 DTO，不再把 `table_no`、`capabilities_json`、`created_at`
  等数据库行字段透给客户端。
- 删除重复的 demo 菜单数据（`src/data.js` 与 `packages/domain` 中的同一份副本）。
- 订正 README、ARCHITECTURE、INSTALL、TEST_REPORT 中与代码不符的描述：管理端认证实际是
  单一静态 token、仓库结构里若干目录尚不存在、没有 ESLint 与 CI、限流窗口是 5 分钟而非
  60 秒、打印代理章节自相矛盾、交付物和本机路径已失效。
## 2026-08-17

### 契约统一

- 请求体 schema 从「服务端和 contracts 包各写一份」合并为 `src/contracts.js` 一份运行时定义，服务端直接校验它，前端类型描述它。选择纯 JS 是因为服务端要能 `node server/index.mjs` 直接跑，不该为了契约多一个构建步骤。
- 修掉已经发生的漂移：桌号在 contracts 里是 32 位、服务端实际只收 8 位；商品 id 一边有 `maxLength` 一边没有。现在各层一致。
- 新增契约一致性测试：TypeScript 类型能表达的值必须通过运行时校验，schema 拒绝的值必须是类型也拒绝的。改单边会红，不再静默漂移。

### 角色与审计

- 管理端拆成经理 / 服务员 / 厨房三个角色，各自一个共享令牌（`ADMIN_TOKEN` / `STAFF_TOKEN` / `KITCHEN_TOKEN`）。服务员不能改菜单和价格，厨房只能推进订单状态。只配 `ADMIN_TOKEN` 时与之前完全兼容。
- 控制台按角色渲染标签页和按钮，不给出会被 403 的入口。
- 新增操作记录：所有带令牌的写操作和被拒绝的越权尝试都入库（时间、角色、IP、路由、状态、关键字段），经理可在「连接设置」查看。读操作不记，避免淹没。
- 可选令牌在启动时校验：不能与经理令牌相同，生产环境不得短于 32 位。

### 运维与稳定性

- 新增 CI：每个 PR 跑 typecheck、领域单元测试、后端集成测试、生产构建和四视口 Playwright，失败时保留报告。
- 新增 `deploy/`：systemd 服务（API + 每档口打印代理，`Restart=always`）、macOS launchd plist、备份计划说明。断电或崩溃后自动拉起，不再依赖有人手动启动。
- 顾客端和管理台加入错误边界：渲染异常时顾客端显示三语「请叫服务员」并可重新载入，不再白屏；管理台显示错误信息而不是整页空白。
- 补上 jsdom，`@testing-library/react` 从此可用（此前是声明了却跑不起来的依赖），错误边界带组件测试。


## 2026-08-12（下午）

### 结账、税率与过敏原合规

- 商品新增税率字段（10% / 13% / 20%，默认餐食 10%、酒水 20%），菜单价格按含税价处理，订单项保存下单时的税率快照。
- 管理台看板新增「结账」：按桌查看账单明细、按税率拆分净额与税额，一键结账并向前台打印机排入账单小票。账单明确标注为内部账单，不是税务收据。
- 账单小票按打印机语言输出商品名（中/德/英）。
- 过敏原改为奥地利 A–R 代码枚举：后端拒绝非法代码并去重排序，管理台改为勾选框，顾客端显示代码和当前语言说明。

### 桌台登记与令牌

- 新增桌台登记：登记后服务端只接受已登记且启用的桌号，并要求 `x-table-token`；未登记任何桌台时保持开放模式。
- 管理台「连接设置」可登记桌台、更换令牌、删除桌台，并显示每桌的入口链接 `?table=..&k=..`。
- 顾客端从入口链接读取并保存令牌，随下单和服务呼叫一起发送；换桌号时旧令牌不会被带走。
- 服务端统一把桌号规范成大写，菜单顶栏不再写死 `TISCH 08`。

### Verification

- `npm run typecheck`：通过
- `npm run unit`：9/9 通过
- `npm run server:test`：15/15 通过
- `npm test`：44/44 通过
- `npm run build`：通过
- 真机服务实测：未登记桌台可下单；登记桌 12 后无令牌返回 403、带令牌返回 201；账单 25.00(10%) + 3.80(20%) 拆分为净额 22.73/3.17、税额 2.27/0.63，德文账单小票输出 `Interne Rechnung, kein Kassenbeleg`

## 2026-08-12

### 多桌运营修复

- 顾客端不再把所有订单写死到桌号 `08`：桌号来自 `?table=` 或首页设置并持久化到设备，订单、服务呼叫和打印小票都带真实桌号；未配置的设备会显式提示。
- 管理台新增「订单看板」，从服务端读取全店订单、服务呼叫和失败打印任务，可推进订单状态、取消订单、标记呼叫已处理和重新排队打印任务。原先顾客端的员工看板只改本机状态，现已在页面上标明作用范围。
- 服务请求和打印任务的 API 响应统一为 camelCase（`table`/`createdAt`/`printerRole`），与订单契约保持一致。

### 稳定性与安全

- SQLite 连接设置 `busy_timeout`（默认 5000ms）：API 服务与打印代理并发写同一个数据库文件时会等待写锁，而不是立刻抛出 `SQLITE_BUSY`。
- 公开的下单和服务呼叫接口按客户端 IP 限流，超限返回 `429` 和 `Retry-After`。
- 所有响应增加 `X-Content-Type-Options`、`Referrer-Policy` 和 `X-Frame-Options`；上传媒体额外返回锁死的 CSP。
- 缺失的 `/assets/`、`/media/` 资源返回 404，不再回落到 HTML 外壳导致 MIME 报错。

### Verification

- `npm run typecheck`：通过
- `npm run unit`：6/6 通过
- `npm run server:test`：11/11 通过
- `npm test`：44/44 通过（手机/平板横竖屏四种视口）
- `npm run build`：通过

## 2026-08-05

### Production Candidate Hardening

- 离线订单命令持久化到本机，联网后自动退避重试；4xx 业务拒绝不会无限重试。
- 新增 LAN ESC/POS 打印代理，支持任务租约、防重复处理、失败退避和人工重试接口。
- SQLite 使用 `VACUUM INTO` 生成一致性备份并执行 `integrity_check`。
- Android Release 构建必须显式提供签名密钥环境变量，避免误发布 Debug/未签名包。

## 2026-08-02

### Admin Catalog Optimization

- 管理台商品编辑器支持维护结构化点餐选项 JSON，包括加面、去葱、去香菜、去辣和辣度等单选/多选组。
- 商品目录行显示当前商品的选项组数量，避免编辑后无法确认配置是否仍在目录中。
- 管理台保存商品时保留选项配置，并在输入不是合法 JSON 数组时直接提示，不把坏数据提交到后端。
- 管理台选项编辑器增加组、三语名称、选择方式、选项价格的结构校验；后端集成测试覆盖保存后回读。
- 修复并发幂等下单可能因唯一键冲突返回 500 的竞态；修复程序化启动生产服务时短管理员 token 校验不完整的问题。

## 2026-08-01

### Deployment Hardening

- 为后台管理接口增加 TypeBox 请求 schema 校验、统一错误响应和请求 ID。
- 服务端订单与服务请求状态由后端强制执行合法迁移，不能通过直接请求越级。
- 增加生产启动配置校验、优雅停机、SQLite schema version 标记和 Node 版本约束。
- 媒体上传失败或商品不存在时清理已写入文件，避免媒体目录泄漏和孤儿文件。
- 打印机端口和传输方式增加范围校验。
- 新增非法 payload、越级状态和认证限流集成测试。

### Fixed

- 统一卡片详情动画为固定时长的 ease-out，移除 spring、共享布局和内容错峰，减少抖动、跳位和闪烁。
- 修复详情翻转入口错误：移除独立的“详情翻转”按钮，改为点击卡片正面或背面直接触发 3D 翻转。
- 图片和视频详情继续共用同一个 `detail-flip-inner` 动画状态。
- 加减数量、加入购物车和返回正面按钮不会误触卡片翻转。
- 菜单顶部移除语言切换按钮，语言在首页选择后沿用到菜单页。
- 压缩菜单顶栏和分类区的布局留空，关闭 Capacitor 启动延迟并补充视频 metadata 预加载。
- 增加 `prefers-reduced-motion` 支持。

### Added

- 新增结构化菜品选项：加面、不要葱、不要香菜、不要辣椒、不辣、正常辣、加辣椒；选项支持三语、加价和多选/单选规则。
- 订单、购物车、订单历史和打印任务保存并显示选项快照，后端重新校验选项并按选项价格计价。
- 新增照片菜单后端 catalog seed，共 111 条真实菜单数据，覆盖食品、寿司、酒水、咖啡和茶。
- 寿司件数、饮料容量和酒水规格使用独立 SKU，按商品类型自动路由厨房、寿司台和吧台。
- 新增中文、德文、英文语言资源和统一 `productName` 选择逻辑。
- 首页、菜单、购物车、订单页可切换语言，当前语言持久化。
- 新增语言切换和卡片直接翻转的 Playwright 响应式测试。
- 新增根目录 README，明确启动、测试、交互和已知边界。
- 管理 API 增加常量时间 token 比较、错误尝试限流和 `Retry-After` 响应；Android 管理 PIN 增加失败锁定。

### Verification

- `npm run typecheck`：通过
- `npm run unit`：2/2 通过
- `npm test`：更新后重新验证，覆盖手机/平板横竖屏
- `npm run build`：通过
