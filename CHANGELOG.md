# Changelog

## 2026-08-12（下午）

### 结账、税率与过敏原合规

- 商品新增税率字段（10% / 13% / 20%，默认餐食 10%、酒水 20%），菜单价格按含税价处理，订单项保存下单时的税率快照。
- 管理台看板新增「结账」：按桌查看账单明细、按税率拆分净额与税额，一键结账并向前台打印机排入账单小票。账单明确标注为内部账单，不是税务收据。
- 账单小票按打印机语言输出商品名（中/德/英）。
- 过敏原改为奥地利 A–R 代码枚举：后端拒绝非法代码并去重排序，管理台改为勾选框，顾客端显示代码和当前语言说明。

### 桌台登记与令牌

- 新增桌台登记：登记后服务端只接受已登记且启用的桌号，并要求 `x-table-token`；未登记任何桌台时保持开放模式。
- 管理台「连接设置」可登记桌台、更换令牌、删除桌台，并显示每桌的入口链接 `?table=..&k=..`。
- 顾客端从入口链接读取并保存令牌，随下单和服务呼叫一起发送；换桌号时旧令牌不会被带走。
- 服务端统一把桌号规范成大写，菜单顶栏不再写死 `TISCH 08`。

### Verification

- `npm run typecheck`：通过
- `npm run unit`：9/9 通过
- `npm run server:test`：15/15 通过
- `npm test`：44/44 通过
- `npm run build`：通过
- 真机服务实测：未登记桌台可下单；登记桌 12 后无令牌返回 403、带令牌返回 201；账单 25.00(10%) + 3.80(20%) 拆分为净额 22.73/3.17、税额 2.27/0.63，德文账单小票输出 `Interne Rechnung, kein Kassenbeleg`

## 2026-08-12

### 多桌运营修复

- 顾客端不再把所有订单写死到桌号 `08`：桌号来自 `?table=` 或首页设置并持久化到设备，订单、服务呼叫和打印小票都带真实桌号；未配置的设备会显式提示。
- 管理台新增「订单看板」，从服务端读取全店订单、服务呼叫和失败打印任务，可推进订单状态、取消订单、标记呼叫已处理和重新排队打印任务。原先顾客端的员工看板只改本机状态，现已在页面上标明作用范围。
- 服务请求和打印任务的 API 响应统一为 camelCase（`table`/`createdAt`/`printerRole`），与订单契约保持一致。

### 稳定性与安全

- SQLite 连接设置 `busy_timeout`（默认 5000ms）：API 服务与打印代理并发写同一个数据库文件时会等待写锁，而不是立刻抛出 `SQLITE_BUSY`。
- 公开的下单和服务呼叫接口按客户端 IP 限流，超限返回 `429` 和 `Retry-After`。
- 所有响应增加 `X-Content-Type-Options`、`Referrer-Policy` 和 `X-Frame-Options`；上传媒体额外返回锁死的 CSP。
- 缺失的 `/assets/`、`/media/` 资源返回 404，不再回落到 HTML 外壳导致 MIME 报错。

### Verification

- `npm run typecheck`：通过
- `npm run unit`：6/6 通过
- `npm run server:test`：11/11 通过
- `npm test`：44/44 通过（手机/平板横竖屏四种视口）
- `npm run build`：通过

## 2026-08-05

### Production Candidate Hardening

- 离线订单命令持久化到本机，联网后自动退避重试；4xx 业务拒绝不会无限重试。
- 新增 LAN ESC/POS 打印代理，支持任务租约、防重复处理、失败退避和人工重试接口。
- SQLite 使用 `VACUUM INTO` 生成一致性备份并执行 `integrity_check`。
- Android Release 构建必须显式提供签名密钥环境变量，避免误发布 Debug/未签名包。

## 2026-08-02

### Admin Catalog Optimization

- 管理台商品编辑器支持维护结构化点餐选项 JSON，包括加面、去葱、去香菜、去辣和辣度等单选/多选组。
- 商品目录行显示当前商品的选项组数量，避免编辑后无法确认配置是否仍在目录中。
- 管理台保存商品时保留选项配置，并在输入不是合法 JSON 数组时直接提示，不把坏数据提交到后端。
- 管理台选项编辑器增加组、三语名称、选择方式、选项价格的结构校验；后端集成测试覆盖保存后回读。
- 修复并发幂等下单可能因唯一键冲突返回 500 的竞态；修复程序化启动生产服务时短管理员 token 校验不完整的问题。

## 2026-08-01

### Deployment Hardening

- 为后台管理接口增加 TypeBox 请求 schema 校验、统一错误响应和请求 ID。
- 服务端订单与服务请求状态由后端强制执行合法迁移，不能通过直接请求越级。
- 增加生产启动配置校验、优雅停机、SQLite schema version 标记和 Node 版本约束。
- 媒体上传失败或商品不存在时清理已写入文件，避免媒体目录泄漏和孤儿文件。
- 打印机端口和传输方式增加范围校验。
- 新增非法 payload、越级状态和认证限流集成测试。

### Fixed

- 统一卡片详情动画为固定时长的 ease-out，移除 spring、共享布局和内容错峰，减少抖动、跳位和闪烁。
- 修复详情翻转入口错误：移除独立的“详情翻转”按钮，改为点击卡片正面或背面直接触发 3D 翻转。
- 图片和视频详情继续共用同一个 `detail-flip-inner` 动画状态。
- 加减数量、加入购物车和返回正面按钮不会误触卡片翻转。
- 菜单顶部移除语言切换按钮，语言在首页选择后沿用到菜单页。
- 压缩菜单顶栏和分类区的布局留空，关闭 Capacitor 启动延迟并补充视频 metadata 预加载。
- 增加 `prefers-reduced-motion` 支持。

### Added

- 新增结构化菜品选项：加面、不要葱、不要香菜、不要辣椒、不辣、正常辣、加辣椒；选项支持三语、加价和多选/单选规则。
- 订单、购物车、订单历史和打印任务保存并显示选项快照，后端重新校验选项并按选项价格计价。
- 新增照片菜单后端 catalog seed，共 111 条真实菜单数据，覆盖食品、寿司、酒水、咖啡和茶。
- 寿司件数、饮料容量和酒水规格使用独立 SKU，按商品类型自动路由厨房、寿司台和吧台。
- 新增中文、德文、英文语言资源和统一 `productName` 选择逻辑。
- 首页、菜单、购物车、订单页可切换语言，当前语言持久化。
- 新增语言切换和卡片直接翻转的 Playwright 响应式测试。
- 新增根目录 README，明确启动、测试、交互和已知边界。
- 管理 API 增加常量时间 token 比较、错误尝试限流和 `Retry-After` 响应；Android 管理 PIN 增加失败锁定。

### Verification

- `npm run typecheck`：通过
- `npm run unit`：2/2 通过
- `npm test`：更新后重新验证，覆盖手机/平板横竖屏
- `npm run build`：通过
